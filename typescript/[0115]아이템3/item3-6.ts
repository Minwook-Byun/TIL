function setLight(value: boolean) {
    switch (value) {
        case true:
            // turnOn();
            break

        case false:
            // turnOff();
            break
        
            default:
            console.log('요게 실행이 될까?')
    }
}

